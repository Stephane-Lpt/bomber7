package com.bomber7.core.model.square;

/**
 * The {@code BombType} enum represents the different types of bombs that can be
 * used in the game. Each type has distinct characteristics and behaviors.
 */
public enum BombType {
    /** Time-based bomb. */
    TIME,
    /** Trigger-based bomb. */
    TRIGGER
}
